# QNNCV — dependencies as actually installed in the conda env 'qnncv'
# (Windows, Python 3.11.14). Recorded 2026-07-13 from `pip list` of that
# env; no installs or upgrades were performed. These exact versions are
# the ones all golden tests and baseline results were produced with.

# Core scientific computing
numpy==2.0.2
scipy==1.15.3
tensorflow==2.18.0

# Quantum computing
# NOTE: SF 0.23.0 imports scipy.integrate.simps (removed in SciPy 1.14+);
# the entry points alias simps=simpson before importing SF.
StrawberryFields==0.23.0

# Data visualization and analysis
matplotlib==3.10.0
pandas==2.2.2
seaborn==0.13.2

# Machine learning utilities
scikit-learn==1.6.1

# Progress bars and utilities
tqdm==4.67.1

# System utilities
psutil==7.0.0

# Configuration and data handling
PyYAML==6.0.3

# Notebook support (installed in the env; optional for training)
jupyter==1.1.1
ipykernel==6.31.0

# NOTE: pytest/sphinx are NOT installed in the qnncv env — the golden
# suites (tests/golden/) are plain-assert scripts run via run_all.py and
# need no test framework.
